import express from 'express';import gymRoutes from './routes/gym.js';import productRoutes from './routes/product.js';import categoryRoutes from './routes/category.js';const app = express();app.use(express.json());app.use('/api/gym', gymRoutes);app.use('/api/product', productRoutes);app.use('/api/category', categoryRoutes);const port = 3001;app.listen(port, () => {console.log(`Server listening on port ${port}`);});